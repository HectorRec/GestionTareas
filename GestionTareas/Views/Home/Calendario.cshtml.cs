//using FrontGestionTareas.Models;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.RazorPages;

namespace FrontGestionTareas.Pages
{
    public class CalendarioModel : PageModel
    {

        //public JsonResult OnGetTasks()
        //{   
            //  Aqui se obtienen las tares (por defecto esta en memoria)
           // var eventos = TareasModel._tareasDb.Select(t => new
          //  {
         //       id = t.ID,
         //       title = t.titulo,
         //       start = t.fecha_limite.ToString("yyyy-MM-ddTHH:mm:ss"),
         //       descripcion = t.descripcion 
        //    }).ToList();

          //  return new JsonResult(eventos);
       // }
    }


}
